/* ==========================================================
   REMADEF PLATFORM
   Shutdown Bootstrap
   File: src/bootstrap/shutdown.ts
========================================================== */

import Shutdown from "../core/shutdown";

export function initializeShutdown(): void {

    Shutdown.init();

}

export default initializeShutdown;
